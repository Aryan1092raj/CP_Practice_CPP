#include <bits/stdc++.h>
using namespace std;
using ll = long long ;
int main(){
    ios::sync_with_stdio(false);
	cin.tie(nullptr);
    ll t ; cin >> t;
    while(t--){
        ll n ;  cin >> n;
        vector <ll> arr(n);
        for(int i = 0 ; i < n ;++i){
            cin >> arr[i];
        }
        vector <ll> cost(n);
        for(int i = 0 ; i < n ;++i){
            cin >> cost[i];
        }
        
    }
}