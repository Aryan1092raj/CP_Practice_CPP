#include <bits/stdc++.h>
using namespace std;
const  int N = 99999;
int arr[N];
for(int i = 0 ; i < N ; ++i){
    if(arr[i] % 10 == 0){
        
    }
}

int main(){
    ios::sync_with_stdio(false);
	cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        int cnt = 0;
        if
    }
}